# claude-agent-relay-guard

Claude Code のサブエージェント運用で起きる「再委譲」と「多段リレー」を、プロンプトと
hooks の多層防御で止めるための最小構成サンプルです。詳細は [README](./README.md) を参照。

## エージェント委譲ルール

メインセッションは設計・統括・結果検証に徹し、作業はサブエージェントに委譲する。
下記は**役割の対応表であり、全タスクに強制する固定パイプラインではない**。実際に必要な
フェーズにだけ、そのフェーズで必要な作業内容を渡す:

- 複数ファイルにまたがる調査・検索 → `investigator`
- 設計確定後の実装(コード編集・テスト・lint) → `implementer`
- コミット前のコードレビュー → `reviewer`

運用原則:

- **同じ作業内容を複数のエージェントに順番にリレーしない。** 1つのタスクで各役割の
  エージェントを使うのは最大1回ずつで、渡すプロンプトはフェーズごとに異なる内容
  (調査依頼・実装指示・レビュー観点)にする。前段の結果を丸ごと次段の課題として
  再送するのは禁止。
- **互いに独立したタスクが複数あるときは、1つのメッセージで複数のAgent呼び出しを
  同時に発行して並列実行する**(直列に1つずつ起動しない)。依存関係があるものだけ
  順番に実行する。
- 不要なフェーズは飛ばす。調査が不要なら `investigator` を起動しない。レビューは
  コミット前に1回だけ。
- 単一ファイルの軽微な修正はメインセッションが直接行ってよい。
- 上記のリレー禁止はプロンプト指示に加え、hook(`agent-relay-guard`)が機械的にも
  拒否する。判定単位は**タスク(ユーザーの1指示)**で、ユーザーの次の発言で履歴は
  リセットされる(`agent-turn-reset` hook)。同一セッションでも別タスクなら同じ役割を
  再び呼び出せる。
